import { AppPageDirective } from './app-page.directive';

describe('AppPageDirective', () => {
  it('should create an instance', () => {
    const directive = new AppPageDirective();
    expect(directive).toBeTruthy();
  });
});
